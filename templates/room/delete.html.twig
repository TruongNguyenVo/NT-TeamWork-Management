{# <form method="post" action="{{ path('app_room_delete', {'id': room.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ room.id) }}">
    <button class="btn">Delete</button>
</form> #}

{% extends 'base.html.twig' %}

{% block title %}Phòng {{room.name}}{% endblock %}

{% block body %}
    <div class="container">
            <div class="page-inner">
              <div class="page-header">
                <h3 class="fw-bold mb-3">NTTeam</h3>
                <ul class="breadcrumbs mb-3">
                  <li class="nav-home">
                    <a href="#">
                      <i class="icon-home"></i>
                    </a>
                  </li>
                  <li class="separator">
                    <i class="icon-arrow-right"></i>
                  </li>
                  <li class="nav-item">
                    <a href="#">Các nhóm tham gia</a>
                  </li>
                  <li class="separator">
                    <i class="icon-arrow-right"></i>
                  </li>
                  <li class="nav-item">
                    <a href="#">{{room.name}}</a>
                  </li>
                </ul>
              </div>
                {{ form_start(form, {'action': path('app_room_delete', {'id': room.id}), 'method': 'POST', 'attr': {'class': 'd-inline',  'onsubmit': 'return confirm("Bạn có chắc là xóa phòng không?");'}}) }}
                  <div class="mb-3">
                    <label for="name" class="form-label">Tên nhóm</label>
                    {{ form_widget(form.name, {'attr': {'class': 'form-control'}}) }}
                    {{ form_errors(form.name) }}
                  </div>
                    <div class="mb-3">
                        <label for="office" class="form-label">Mật khẩu</label>
                        {{ form_widget(form.password, {'attr': {'class': 'form-control'}}) }}
                        {{ form_errors(form.password) }}
                    </div>
                  <div class="mb-3">
                    <label for="office" class="form-label">Mô tả</label>
                    {{ form_widget(form.description, {'attr': {'class': 'form-control', 'rows':'5'}}) }}
                    {{ form_errors(form.description) }}
                  </div>
                   <div class="mb-3-md">
                        {# Trở lại trang chính #}
                        <a href="{{ path('app_home') }}" class="btn btn-primary">Trở lại</a>
                        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ room.id) }}">
                        {# Nút Chỉnh Sửa (Submit Form) #}
                        <button type="submit" class="btn btn-danger"> Giải tán nhóm</button>
                        
                    </div>
              </div>  
            {{form_end(form)}}
            </div>
            </div>
{% endblock %}

